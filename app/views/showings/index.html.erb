
  <% @showings.each do |showing| %>
  <h2><%= link_to showing.theater.name, theater_showings_path(showing) %></h2>
<ul>
    <li>
      <%= showing.time %>
    </li>

    <li>
      <%= showing.movie.name %>
      </li>
    <br>

</ul>
  <% end %>
<h4>Know something we don't know? <%= link_to 'Click here', new_movie_path %> to add another showtime</h4>
