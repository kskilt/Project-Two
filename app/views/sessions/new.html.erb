<%= form_with scope: :users, url: login_path, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "log in" %><br>
  Or <%= link_to(controller: 'users', action: 'new') { 'sign up' } %>
<% end %>
